#!/bin/bash
# فایل start.sh

echo "🚀 در حال راه‌اندازی ربات‌ها..."

# اجرای ربات Normal در پس‌زمینه
python normal_bot.py &
NORMAL_PID=$!

# اگر فایل Pro وجود دارد، اجرا کن
if [ -f "pro_bot.py" ]; then
    python pro_bot.py &
    PRO_PID=$!
    echo "✅ هر دو ربات اجرا شدند"
else
    echo "⚠️ فقط ربات Normal اجرا شد (pro_bot.py یافت نشد)"
fi

# نگه داشتن کانتینر فعال
echo "📡 سرویس فعال است..."
wait $NORMAL_PID
